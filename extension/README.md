# X Video Vault Collector

This Manifest V3 extension scans only video/GIF Posts already rendered in the
signed-in browser's current X page. It sends metadata to the Vault URL that the
user configures. It does not receive an X password, use X API credentials, or
download video files.

Load this directory from chrome://extensions or edge://extensions with
Developer mode enabled and Load unpacked.
